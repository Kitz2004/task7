# Sales Analysis with SQLite and Python

## Objective
Extract sales summaries (total quantity, revenue) from an SQLite database and visualize results.

## Steps
1. Created `sales_data.db` with a `sales` table.
2. Queried data using SQL (`GROUP BY`, aggregations).
3. Used pandas for analysis and matplotlib for visualization
